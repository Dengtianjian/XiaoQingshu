<template name="qa-post">
  <view class="post-body-description">{{ content }}</view>
  <image class="post-body-image" src="{{ images[0] }}" wx:if="{{ images&&images.length>0 }}" lazy-load></image>
</template>