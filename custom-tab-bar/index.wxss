.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 108rpx;
  background: white;
  border-top:1px solid rgb(241, 241, 241);
  display: flex;
  padding-bottom: env(safe-area-inset-bottom);
}

.tab-bar-item {
  flex: 1;
  text-align: center;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
}

.tab-bar-item cover-image {
  width: 50rpx;
  height: 50rpx;
}
.tab-bar-item-active cover-image {
  background:red;
  border-color:blue;
}

.tab-bar-item cover-view {
  margin-top:10rpx;
  font-size: 20rpx;
}
